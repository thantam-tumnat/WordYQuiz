# Python virtualenv (accidentally committed before)
API/.venv/
.venv/
__pycache__/
*.pyc

# Real config with DB credentials (use config.example.yaml as template)
API/config.yaml

# Real compose with DB credentials (use docker-compose.example.yml as template)
docker-compose.yml

# Go build artifacts
*.exe
API/API
API/api
API/main

# Editor / OS
.DS_Store
Thumbs.db
